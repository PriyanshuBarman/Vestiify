name: SIP Installment Order Placement Cron

on:
  schedule:
    - cron: "30 20 * * *" # 2:00 AM IST
    - cron: "30 21 * * *" # 3:00 AM IST
  workflow_dispatch: # Allow manual trigger

jobs:
  sip-installment-order-cron:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: SIP Installment Order Placement
        id: update-script
        run: node scripts/tasks/processSipInstallmentOrders.task.js
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
